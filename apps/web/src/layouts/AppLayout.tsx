import { NavLink, Outlet, useNavigate } from "react-router-dom"; import { Activity,FileText,LogOut,Map,Network,Search,Shield,Sun,Moon } from "lucide-react"; import { BrandMark } from "../components/common/BrandMark"; import { useAuth } from "../hooks/useAuth"; import { useTheme } from "../context/ThemeContext";
const links=[['/dashboard','Dashboard',Activity],['/iocs','IOC Intel',Search],['/investigate','Investigate',Shield],['/attack-dna','Attack DNA',Network],['/crime-map','Crime Map',Map],['/reports','Reports',FileText]] as const;
export function AppLayout(){const {session,logout}=useAuth();const {theme,toggleTheme}=useTheme();const nav=useNavigate();return <div className="min-h-screen md:flex"><aside className="app-sidebar no-print w-full p-4 md:min-h-screen md:w-60"><div className="mb-8"><BrandMark/></div><nav className="flex gap-1 overflow-auto md:block">{links.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`nav-link ${isActive?'nav-link-active':''}`}><Icon size={16}/>{label}</NavLink>)}</nav></aside><section className="min-w-0 flex-1"><header className="app-header no-print flex items-center justify-end gap-3 px-6 py-3 text-sm"><span className="hidden sm:inline">{session?.user.name || session?.user.email}</span><button aria-label={`Switch to ${theme === "dark" ? "light" : "dark"} theme`} onClick={toggleTheme} className="theme-toggle">{theme === "dark" ? <Sun size={17}/> : <Moon size={17}/>}<span>{theme === "dark" ? "Light" : "Dark"}</span></button><button onClick={()=>{logout();nav('/login')}} className="flex items-center gap-1 hover:text-cyan-300"><LogOut size={16}/>Logout</button></header><main className="p-5 md:p-8"><Outlet/></main></section></div>}
