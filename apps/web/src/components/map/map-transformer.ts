import type { ThreatLocation } from "@cybertrace/shared";
export type ThreatMarker=ThreatLocation & { radius:number };
export function transformLocationsToMarkers(locations: readonly Partial<ThreatLocation>[] | undefined | null): ThreatMarker[] { if (!locations) return []; const grouped=new Map<string,ThreatLocation>(); for(const location of locations.filter((l):l is ThreatLocation=>typeof l.latitude==="number"&&Number.isFinite(l.latitude)&&l.latitude>=-90&&l.latitude<=90&&typeof l.longitude==="number"&&Number.isFinite(l.longitude)&&l.longitude>=-180&&l.longitude<=180)){const key=`${location.countryCode||location.country}:${location.latitude}:${location.longitude}`;const previous=grouped.get(key);grouped.set(key,previous?{...previous,threatCount:previous.threatCount+location.threatCount,criticalCount:previous.criticalCount+location.criticalCount,iocIds:[...previous.iocIds,...location.iocIds]}:location);}return [...grouped.values()].map(l=>({...l,radius:Math.max(8,Math.min(30,8+Math.sqrt(l.threatCount||0)*5))})); }
