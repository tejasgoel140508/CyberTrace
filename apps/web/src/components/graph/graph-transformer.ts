import type { Edge, Node } from "@xyflow/react";
type Source = { graph?: {nodes?: {id:string;label:string;type:string;severity?:string;metadata?:Record<string,unknown>}[];edges?: {id:string;source:string;target:string;label:string;score?:number}[]}; iocs?: {id:string;value:string;type:string}[]; relationships?: {id?:string;sourceIocId:string;targetIocId:string;relationshipType:string;score?:number}[] };
export function transformInvestigationToGraph(investigation: Source): {nodes: Node[]; edges: Edge[]} { const graph=investigation.graph; const nodes=(graph?.nodes?.length ? graph.nodes : investigation.iocs || []).map((item,index)=>({id:item.id,position:{x:(index%3)*280,y:Math.floor(index/3)*180},data:{label:"label" in item ? item.label : item.value,type:item.type,severity:"severity" in item ? item.severity : undefined,metadata:"metadata" in item ? item.metadata : undefined},type:"threat"})); const links=(graph?.edges || investigation.relationships || []).map(edge=>({ ...edge, source: "source" in edge ? edge.source : edge.sourceIocId, target: "target" in edge ? edge.target : edge.targetIocId, label: "label" in edge ? edge.label : edge.relationshipType })); const edges=links.filter(edge=>nodes.some(n=>n.id===edge.source)&&nodes.some(n=>n.id===edge.target)).map((edge,index)=>({id:edge.id || `edge-${index}`,source:edge.source,target:edge.target,label:edge.label.replaceAll("_"," "),animated:(edge.score||0)>=80,style:{stroke:(edge.score||0)>=80?"#22d3ee":"#64748b",strokeWidth:2},labelStyle:{fill:"#94a3b8",fontSize:10,fontWeight:700},labelBgStyle:{fill:"#0f172a",fillOpacity:.9},labelBgPadding:[5,3] as [number,number]})); return {nodes,edges}; }
